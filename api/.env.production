# ═══════════════════════════════════════════════════════════════
# CURITRANSPORTE API - CONFIGURACIÓN PRODUCCIÓN EC2
# ═══════════════════════════════════════════════════════════════
# Copia este archivo a .env en tu EC2:
# cp .env.production .env
# nano .env  (edita con tus valores reales)
# ═══════════════════════════════════════════════════════════════

# ─── Servidor ─────────────────────────────────────────────────
NODE_ENV=production
PORT=3001

# URL base de la API (IP Elástica o dominio)
# Esto se usa en Swagger para documentación
API_BASE_URL=http://13.222.211.133:3001

# ─── Base de Datos PostgreSQL (RDS) ───────────────────────────
# Usa el endpoint de RDS (IP privada dentro de la VPC)
DB_HOST=curicotransporte.cxxavlgth5q2.us-east-1.rds.amazonaws.com
DB_PORT=5432
DB_NAME=postgres
DB_USER=leica
DB_PASSWORD=leica12345
DB_SCHEMA=tesis

# ─── JWT ──────────────────────────────────────────────────────
# IMPORTANTE: Cambia esto por una clave segura de al menos 64 caracteres
# Puedes generar una con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=LEICA123_SuPerSeCreTa_MuyLargaYSeguraParaProduccion_AbC9DeFgHiJkLmNoPqRsTuVwXyZ0123456789
JWT_EXPIRES_IN=24h

# ─── Mock Data ────────────────────────────────────────────────
USE_MOCK_DATA=false

# ─── HTTPS/TLS ────────────────────────────────────────────────
# Opción 1: Sin HTTPS (recomendado si usas Nginx como reverse proxy)
ENABLE_HTTPS=false

# Opción 2: HTTPS directo con Let's Encrypt (sin Nginx)
# ENABLE_HTTPS=true
# HTTPS_PORT=443
# TLS_STRICT=false
# SSL_KEY_PATH=/etc/letsencrypt/live/tudominio.com/privkey.pem
# SSL_CERT_PATH=/etc/letsencrypt/live/tudominio.com/fullchain.pem
# SSL_CA_PATH=/etc/letsencrypt/live/tudominio.com/chain.pem

# ─── CORS ─────────────────────────────────────────────────────
# Para apps móviles: dejar vacío o con * (permiten cualquier origen)
# Para frontend web específico: separar por comas
ALLOWED_ORIGINS=*

# ─── Scheduler de Reportes ────────────────────────────────────
ENABLE_SCHEDULER=true

# ─── Logging ──────────────────────────────────────────────────
# En producción puedes cambiar a 'combined' para más detalles
LOG_FORMAT=combined
